#include<iostream>
using namespace std;
int main()
{
  int n,a[10],i;
  float sum=0.0,average;
  cout<<"enter the size of array : \n";
  cin>>n;
  for(i=0;i<n;i++)
  {
    cin>>a[i];
    sum+=a[i];
  }
  average = sum / n;
  cout<<"the average of array = "<<average<<endl;
  return 0;
}
